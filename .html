<!DOCTYPE html>
<html lang="hi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>कक्षा 8 हिंदी (किसलय) - बिहार बोर्ड | Solution Study Center</title>

    <!-- Google Fonts -->
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Noto+Sans+Devanagari:wght@400;500;700&display=swap"
        rel="stylesheet">

    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        /* ============================================
           GLOBAL STYLES & CSS VARIABLES
           ============================================ */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            --primary: #1e3c72;
            --primary-dark: #0a2a4a;
            --secondary: #ff9800;
            --white: #ffffff;
            --transition: all 0.3s ease;

            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
            line-height: 1.6;
            font-family: 'Poppins', 'Noto Sans Devanagari', sans-serif;
            min-height: 100vh;
            padding-top: 80px;
        }

        /* ============================================
           HEADER / NAVBAR STYLES (Fixed at top)
           ============================================ */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            padding: 15px 0;
            z-index: 1000;
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.2);
        }

        .container {
            width: 90%;
            max-width: 1400px;
            margin: auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            color: var(--white);
            font-size: 26px;
            font-weight: 700;
            letter-spacing: 1px;
        }

        .logo span {
            color: var(--secondary);
            font-weight: 800;
        }

        .navbar ul {
            display: flex;
            gap: 15px;
            list-style: none;
        }

        .navbar ul li a {
            color: var(--white);
            text-decoration: none;
            font-size: 16px;
            font-weight: 500;
            padding: 8px 16px;
            border-radius: 30px;
            transition: var(--transition);
        }

        .navbar ul li a:hover,
        .navbar ul li a.active {
            background: var(--secondary);
            color: var(--primary-dark);
        }

        .menu-icon {
            display: none;
            font-size: 28px;
            color: var(--white);
            cursor: pointer;
        }

        /* ============================================
           BACK BUTTON SECTION (Below Header)
           Navigates to courses.html page when clicked
           ============================================ */
        .back-button-section {
            width: 90%;
            max-width: 1400px;
            margin: 20px auto 0 auto;
            padding: 0 15px;
        }

        .back-btn-container {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            border-radius: 60px;
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 8px 24px 8px 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: var(--transition);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .back-btn-container:hover {
            background: rgba(255, 255, 255, 0.25);
            transform: translateX(-5px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }

        /* Back button link styling - navigates to courses page */
        .back-btn-main {
            background: none;
            border: none;
            color: white;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            font-family: 'Poppins', 'Noto Sans Devanagari', sans-serif;
            letter-spacing: 0.5px;
            text-decoration: none;
        }

        .back-btn-main i {
            font-size: 18px;
            transition: transform 0.3s ease;
        }

        .back-btn-container:hover .back-btn-main i {
            transform: translateX(-3px);
        }

        .back-hint {
            font-size: 12px;
            color: rgba(255, 255, 255, 0.8);
            border-left: 1px solid rgba(255, 255, 255, 0.4);
            padding-left: 12px;
            margin-left: 5px;
        }

        /* ============================================
           MAIN CONTENT STYLES
           ============================================ */
        .main-container {
            width: 90%;
            max-width: 1400px;
            margin: 30px auto;
            padding: 0 15px;
        }

        /* Welcome Section */
        .welcome-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px;
            border-radius: 20px;
            margin-bottom: 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .welcome-section h1 {
            font-size: 2.5em;
            margin-bottom: 15px;
        }

        .welcome-section p {
            font-size: 1.2em;
            opacity: 0.9;
        }

        /* Section Header */
        .section-header {
            text-align: center;
            margin: 40px 0 30px;
        }

        .section-header h2 {
            font-size: 32px;
            color: white;
            position: relative;
            display: inline-block;
            padding-bottom: 10px;
        }

        .section-header h2::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: var(--secondary);
            border-radius: 2px;
        }

        /* Chapter Grid - Cards Layout */
        .chapter-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
            margin-top: 20px;
        }

        .chapter-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            transition: all 0.3s;
            cursor: pointer;
            position: relative;
            border: 2px solid transparent;
        }

        .chapter-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            border-color: var(--secondary);
        }

        .chapter-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .chapter-number {
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            font-weight: bold;
            border: 2px solid white;
        }

        .chapter-title {
            font-size: 18px;
            font-weight: 600;
            flex: 1;
        }

        .chapter-body {
            padding: 20px;
        }

        .chapter-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            color: #666;
            font-size: 14px;
        }

        .chapter-meta i {
            margin-right: 5px;
            color: #764ba2;
        }

        .chapter-actions {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }

        .action-btn {
            flex: 1;
            padding: 12px;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            text-decoration: none;
            font-size: 14px;
        }

        .read-btn {
            background: #667eea;
            color: white;
        }

        .read-btn:hover {
            background: #5a67d8;
            transform: scale(1.05);
        }

        .pdf-btn {
            background: #dc3545;
            color: white;
        }

        .pdf-btn:hover {
            background: #c82333;
            transform: scale(1.05);
        }

        /* Content Section - Where PDF Viewer Appears */
        .content-section {
            background: white;
            border-radius: 20px;
            padding: 30px;
            margin: 30px 0;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
        }

        #chapterContent {
            font-size: 17px;
            line-height: 1.8;
            text-align: center;
            padding: 40px 20px;
        }

        #chapterContent h2 {
            color: #0b4d3b;
            margin-bottom: 20px;
            font-size: 28px;
        }

        #chapterContent p {
            color: #666;
            font-size: 18px;
        }

        /* PDF Viewer Styles */
        .pdf-viewer {
            background: white;
            padding: 20px;
            border-radius: 15px;
        }

        .pdf-viewer iframe {
            width: 100%;
            height: 600px;
            border: none;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .chapter-pdf-viewer {
            width: 100%;
            height: 700px;
            border: none;
            border-radius: 10px;
            margin-top: 20px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        /* Tabs Section */
        .tabs {
            display: flex;
            gap: 10px;
            margin: 30px 0 20px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .tab {
            padding: 12px 25px;
            background: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: 600;
            font-size: 16px;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
        }

        .tab:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .tab.active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .tab-content {
            display: none;
            animation: fadeIn 0.5s;
        }

        .tab-content.active {
            display: block;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Video Grid */
        .video-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }

        .video-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
        }

        .video-card iframe {
            width: 100%;
            height: 200px;
            border: none;
        }

        .video-info {
            padding: 15px;
        }

        .video-info h4 {
            color: #333;
            margin-bottom: 5px;
        }

        .video-info p {
            color: #666;
            font-size: 14px;
        }

        /* Quiz Section */
        .quiz-container {
            background: white;
            padding: 30px;
            border-radius: 20px;
        }

        .quiz-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .quiz-header h2 {
            color: #333;
            font-size: 24px;
        }

        .quiz-header h2 i {
            color: #764ba2;
            margin-right: 10px;
        }

        .quiz-progress {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 8px 20px;
            border-radius: 50px;
            font-weight: 600;
        }

        .question {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 20px;
        }

        .question h3 {
            color: #333;
            margin-bottom: 20px;
            font-size: 20px;
        }

        .options {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .option {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 12px 20px;
            background: white;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s;
            border: 1px solid #e0e0e0;
        }

        .option:hover {
            background: #e9ecef;
            transform: translateX(5px);
            border-color: #764ba2;
        }

        .option input[type="radio"] {
            width: 18px;
            height: 18px;
            cursor: pointer;
        }

        .quiz-footer {
            display: flex;
            justify-content: space-between;
            margin-top: 25px;
            flex-wrap: wrap;
            gap: 10px;
        }

        .quiz-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-size: 16px;
        }

        .quiz-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .quiz-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }

        /* Grammar Grid */
        .grammar-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .grammar-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
            transition: all 0.3s;
            cursor: pointer;
        }

        .grammar-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.15);
        }

        .grammar-card i {
            font-size: 40px;
            color: #764ba2;
            margin-bottom: 15px;
        }

        .grammar-card h3 {
            color: #333;
            margin-bottom: 10px;
        }

        .grammar-card p {
            color: #666;
            font-size: 14px;
        }

        /* Footer Section */
        .main-footer {
            background: linear-gradient(135deg, var(--primary-dark), var(--primary));
            color: var(--white);
            text-align: center;
            padding: 40px 20px;
            margin-top: 40px;
        }

        .social-icons {
            margin-top: 20px;
        }

        .social-icons .icon {
            display: inline-flex;
            width: 45px;
            height: 45px;
            background: rgba(255, 255, 255, 0.1);
            color: var(--white);
            border-radius: 50%;
            align-items: center;
            justify-content: center;
            margin: 0 8px;
            font-size: 20px;
            text-decoration: none;
            transition: var(--transition);
        }

        .social-icons .icon:hover {
            transform: translateY(-5px) rotate(360deg);
        }

        .fb:hover { background: #1877f2; }
        .insta:hover { background: #e1306c; }
        .yt:hover { background: #ff0000; }
        .wa:hover { background: #25d366; }

        /* WhatsApp Float Button */
        .whatsapp-float {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: #25d366;
            color: white;
            width: 60px;
            height: 60px;
            border-radius: 50%;
            text-align: center;
            font-size: 30px;
            line-height: 60px;
            box-shadow: 0 5px 20px rgba(37, 211, 102, 0.3);
            z-index: 999;
            transition: all 0.3s;
            animation: pulse 2s infinite;
            text-decoration: none;
        }

        .whatsapp-float:hover {
            transform: scale(1.1);
            background: #20ba5c;
        }

        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(37, 211, 102, 0.7); }
            70% { box-shadow: 0 0 0 15px rgba(37, 211, 102, 0); }
            100% { box-shadow: 0 0 0 0 rgba(37, 211, 102, 0); }
        }

        /* Back to Top Button */
        .back-to-top {
            position: fixed;
            bottom: 100px;
            right: 30px;
            background: var(--secondary);
            color: var(--primary-dark);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s;
            z-index: 999;
            font-size: 20px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
        }

        .back-to-top.show {
            opacity: 1;
            visibility: visible;
        }

        .back-to-top:hover {
            transform: translateY(-5px);
            background: #ffaa33;
        }

        /* Mobile Responsive Styles */
        @media (max-width: 768px) {
            .menu-icon {
                display: block;
            }
            .navbar ul {
                display: none;
                position: absolute;
                top: 70px;
                left: 0;
                width: 100%;
                background: linear-gradient(135deg, var(--primary), var(--primary-dark));
                flex-direction: column;
                padding: 20px 0;
                text-align: center;
                box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
            }
            .navbar ul.show {
                display: flex;
            }
            .navbar ul li {
                margin: 10px 0;
            }
            .chapter-grid {
                grid-template-columns: 1fr;
            }
            .welcome-section h1 {
                font-size: 1.8em;
            }
            .welcome-section {
                padding: 30px 20px;
            }
            .chapter-pdf-viewer {
                height: 500px;
            }
            .tabs {
                flex-direction: column;
                align-items: stretch;
            }
            .tab {
                width: 100%;
                justify-content: center;
            }
            .quiz-footer {
                flex-direction: column;
            }
            .quiz-btn {
                width: 100%;
                justify-content: center;
            }
            .back-button-section {
                margin: 15px auto 0 auto;
            }
            .back-btn-container {
                padding: 6px 18px 6px 15px;
            }
            .back-hint {
                display: none;
            }
        }
    </style>
</head>

<body>
    <!-- ============================================
         HEADER SECTION - Fixed Navigation Bar
         ============================================ -->
    <nav class="navbar">
        <div class="container">
            <div class="logo">SOLUTION STUDY <span>CENTER</span></div>
            <div class="menu-icon" onclick="toggleMenu()">☰</div>
            <ul id="menu">
                <li><a href="index.html">Home</a></li>
                <li><a href="about.html">About</a></li>
                <li><a href="course.html">Courses</a></li>
                <li><a href="admission.html">Admissions</a></li>
                <li><a href="gallery.html">Gallery</a></li>
                <li><a href="contact.html">Contact</a></li>
            </ul>
        </div>
    </nav>

    <!-- ============================================
         BACK BUTTON SECTION (Placed just below header)
         This button navigates to courses.html page
         ============================================ -->
    <div class="back-button-section">
        <div class="back-btn-container">
            <!-- Back button as an anchor tag that redirects to courses page -->
            <a href="course.html" class="back-btn-main">
                <i class="fas fa-arrow-left"></i>
                <span>वापस जाएं</span>
            </a>
        </div>
    </div>

    <!-- ============================================
         MAIN CONTENT AREA
      ============================================ -->
    <div class="main-container">
        <!-- Welcome Section -->
        <div class="welcome-section">
            <h1>📚 बिहार बोर्ड कक्षा 8 हिंदी (किसलय)</h1>
            <p>सभी 23 अध्याय आकर्षक कार्ड व्यू में, PDF डाउनलोड के साथ</p>
        </div>

        <!-- Section Header -->
        <div class="section-header">
            <h2>📖 किसलय भाग 3 - सभी अध्याय</h2>
        </div>

        <!-- Chapter Grid - Dynamic Chapters Loaded Here -->
        <div class="chapter-grid" id="chapterGrid">
            <!-- Chapters will be loaded by JavaScript -->
        </div>

        <!-- Content Section with PDF Viewer -->
        <div class="content-section" id="chapterContentSection">
            <div id="chapterContent">
                <h2>📖 अध्याय पढ़ने के लिए कार्ड पर क्लिक करें</h2>
                <p>कृपया किसी अध्याय के कार्ड पर "पढ़ें" या "PDF" बटन पर क्लिक करें।</p>
            </div>
            <!-- PDF Viewer for Chapter (Initially Hidden) -->
            <div id="chapterPDFContainer" style="display: none;">
                <h2 id="chapterPDFTitle" style="color: #0b4d3b; margin-bottom: 20px; text-align: center;"></h2>
                <iframe class="chapter-pdf-viewer" id="chapterPDFViewer" src=""></iframe>
            </div>
        </div>

        <!-- Tabs Section -->
        <div class="tabs">
            <button class="tab active" onclick="switchTab('video', event)">
                <i class="fas fa-play"></i> वीडियो
            </button>
            <button class="tab" onclick="switchTab('pdf', event)">
                <i class="fas fa-file-pdf"></i> पीडीएफ व्यूअर
            </button>
            <button class="tab" onclick="switchTab('quiz', event)">
                <i class="fas fa-puzzle-piece"></i> क्विज टेस्ट
            </button>
            <button class="tab" onclick="switchTab('grammar', event)">
                <i class="fas fa-language"></i> व्याकरण
            </button>
        </div>

        <!-- Video Tab Content -->
        <div id="videoTab" class="tab-content active">
            <div class="video-section">
                <div class="video-grid">
                    <div class="video-card">
                        <iframe src="https://www.youtube.com/embed/VIDEO_ID_1" allowfullscreen></iframe>
                        <div class="video-info">
                            <h4>तू जिन्दा है तो - व्याख्या</h4>
                            <p>कविता का सरल भाषा में वर्णन</p>
                        </div>
                    </div>
                    <div class="video-card">
                        <iframe src="https://www.youtube.com/embed/VIDEO_ID_2" allowfullscreen></iframe>
                        <div class="video-info">
                            <h4>ईदगाह - प्रेमचंद</h4>
                            <p>कहानी का रोचक वर्णन</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- PDF Tab Content - General PDF Viewer -->
        <div id="pdfTab" class="tab-content">
            <div class="pdf-viewer">
                <iframe
                    src="https://drive.google.com/viewerng/viewer?embedded=true&url=https://www.mohali.gov.in/sites/default/files/Hindi%20Digdarshika%20class%208.pdf"
                    id="pdfViewer"></iframe>
            </div>
        </div>

        <!-- Quiz Tab Content -->
        <div id="quizTab" class="tab-content">
            <div class="quiz-container">
                <div class="quiz-header">
                    <h2><i class="fas fa-puzzle-piece"></i> हिंदी क्विज</h2>
                    <div class="quiz-progress" id="quizProgress">प्रश्न 1/5</div>
                </div>
                <div id="quizArea"></div>
                <div class="quiz-footer">
                    <button class="quiz-btn" onclick="prevQuestion()" id="prevBtn" disabled>
                        <i class="fas fa-arrow-left"></i> पिछला
                    </button>
                    <button class="quiz-btn" onclick="nextQuestion()" id="nextBtn">
                        अगला <i class="fas fa-arrow-right"></i>
                    </button>
                    <button class="quiz-btn" onclick="checkQuiz()" id="submitBtn" style="display: none;">
                        <i class="fas fa-check"></i> जमा करें
                    </button>
                </div>
                <div id="result" style="display: none;"></div>
            </div>
        </div>

        <!-- Grammar Tab Content -->
        <div id="grammarTab" class="tab-content">
            <div class="grammar-grid">
                <div class="grammar-card"><i class="fas fa-pencil-alt"></i><h3>संज्ञा</h3><p>नाम बताने वाले शब्द</p></div>
                <div class="grammar-card"><i class="fas fa-running"></i><h3>क्रिया</h3><p>कार्य बताने वाले शब्द</p></div>
                <div class="grammar-card"><i class="fas fa-star"></i><h3>विशेषण</h3><p>गुण बताने वाले शब्द</p></div>
                <div class="grammar-card"><i class="fas fa-link"></i><h3>संधि</h3><p>शब्दों का मेल</p></div>
                <div class="grammar-card"><i class="fas fa-random"></i><h3>समास</h3><p>संक्षिप्तीकरण</p></div>
                <div class="grammar-card"><i class="fas fa-comment"></i><h3>मुहावरे</h3><p>लोकोक्तियाँ</p></div>
            </div>
        </div>
    </div>

    <!-- ============================================
         FOOTER SECTION
         ============================================ -->
    <footer class="main-footer">
        <p>© 2026 Solution Study Center Mansi | All Rights Reserved</p>
        <div class="social-icons">
            <a href="https://www.facebook.com/share/1B5dZQDXE5/" class="icon fb" target="_blank"><i class="fab fa-facebook-f"></i></a>
            <a href="https://www.instagram.com/solution_study_center_mansi?igsh=MTdjZTAwZG9xbDR0NQ==" class="icon insta" target="_blank"><i class="fab fa-instagram"></i></a>
            <a href="#" class="icon yt" target="_blank"><i class="fab fa-youtube"></i></a>
            <a href="https://wa.me/917903912699" class="icon wa" target="_blank"><i class="fab fa-whatsapp"></i></a>
        </div>
    </footer>

    <!-- WhatsApp Float Button -->
    <a href="https://wa.me/917903912699" class="whatsapp-float" target="_blank"><i class="fab fa-whatsapp"></i></a>

    <!-- Back to Top Button -->
    <div class="back-to-top" onclick="scrollToTop()" id="backToTop"><i class="fas fa-arrow-up"></i></div>

    <!-- ============================================
         JAVASCRIPT CODE
         ============================================ -->
    <script>
        // ============================================
        // CHAPTER DATA - 23 Chapters of Hindi Book
        // ============================================
        const chapters = [
            { num: 1, title: "तू जिन्दा है तो", color: "#FF6B6B" }, { num: 2, title: "ईदगाह", color: "#4ECDC4" },
            { num: 3, title: "कर्मवीर", color: "#45B7D1" }, { num: 4, title: "बालगोबिन भगत", color: "#96CEB4" },
            { num: 5, title: "हुंडरू का जलप्रपात", color: "#FFEAA7" }, { num: 6, title: "बिहारी के दोहे", color: "#DDA0DD" },
            { num: 7, title: "ठेस", color: "#98D8C8" }, { num: 8, title: "बच्चे की दुआ", color: "#F7DC6F" },
            { num: 9, title: "अशोक का शस्त्र-त्याग", color: "#BB8FCE" }, { num: 10, title: "ईर्ष्या : तू न गई मेरे मन से", color: "#85C1E2" },
            { num: 11, title: "कबीर के पद", color: "#F8C471" }, { num: 12, title: "विक्रमशिला", color: "#82E0AA" },
            { num: 13, title: "दीदी की डायरी", color: "#F1948A" }, { num: 14, title: "पीपल", color: "#7FB3D5" },
            { num: 15, title: "दीनबन्धु 'निराला'", color: "#E59866" }, { num: 16, title: "खेमा", color: "#C39BD3" },
            { num: 17, title: "खुशबू रचते हैं हाथ", color: "#73C6B6" }, { num: 18, title: "हौसले की उड़ान", color: "#F0B27A" },
            { num: 19, title: "जननायक कर्पुरी ठाकुर", color: "#A569BD" }, { num: 20, title: "झाँसी की रानी", color: "#5DADE2" },
            { num: 21, title: "चिकित्सा का चक्कर", color: "#48C9B0" }, { num: 22, title: "सुदामा चरित", color: "#CD6155" },
            { num: 23, title: "राह भटके हिरण के बच्चे", color: "#AEB6BF" }
        ];

        // ============================================
        // PDF LINKS FOR EACH CHAPTER (Google Drive Viewer)
        // ============================================
        const chapterPDFLinks = {};
        for(let i=1; i<=23; i++) {
            chapterPDFLinks[i] = "https://drive.google.com/viewerng/viewer?embedded=true&url=https://www.mohali.gov.in/sites/default/files/Hindi%20Digdarshika%20class%208.pdf";
        }

        // ============================================
        // QUIZ QUESTIONS DATA
        // ============================================
        const questions = [
            { question: "1. 'ईदगाह' कहानी के लेखक कौन हैं?", options: ["प्रेमचंद", "रामवृक्ष बेनीपुरी", "जयशंकर प्रसाद", "मैथिलीशरण गुप्त"], correct: 0 },
            { question: "2. हिन्दी किस लिपि में लिखी जाती है?", options: ["देवनागरी", "रोमन", "उर्दू", "गुरुमुखी"], correct: 0 },
            { question: "3. संज्ञा किसे कहते हैं?", options: ["नाम बताने वाला शब्द", "कार्य बताने वाला शब्द", "गुण बताने वाला शब्द", "समय बताने वाला शब्द"], correct: 0 },
            { question: "4. 'अ' क्या है?", options: ["व्यंजन", "स्वर", "संयुक्ताक्षर", "मात्रा"], correct: 1 },
            { question: "5. 'पढ़ाई' शब्द में कौन सी ध्वनि है?", options: ["स्वर", "व्यंजन", "उभय", "मिश्रित"], correct: 1 }
        ];

        // Quiz State Variables
        let currentQuestion = 0;
        let userAnswers = new Array(questions.length).fill(-1);

        // ============================================
        // FUNCTION: Load All Chapter Cards
        // ============================================
        function loadChapters() {
            const grid = document.getElementById('chapterGrid');
            grid.innerHTML = '';
            chapters.forEach(ch => {
                const card = document.createElement('div');
                card.className = 'chapter-card';
                card.innerHTML = `
                    <div class="chapter-header" style="background: linear-gradient(135deg, ${ch.color} 0%, #764ba2 100%);">
                        <div class="chapter-number">${ch.num}</div>
                        <div class="chapter-title">${ch.title}</div>
                    </div>
                    <div class="chapter-body">
                        <div class="chapter-meta"><span><i class="fas fa-book-open"></i> अध्याय ${ch.num}</span><span><i class="fas fa-clock"></i> 15 min read</span></div>
                        <div class="chapter-actions">
                            <button class="action-btn read-btn" onclick="openChapterPDF(${ch.num})"><i class="fas fa-book-open"></i> पढ़ें</button>
                            <button class="action-btn pdf-btn" onclick="openChapterPDF(${ch.num})"><i class="fas fa-file-pdf"></i> PDF</button>
                        </div>
                    </div>
                `;
                grid.appendChild(card);
            });
        }

        // ============================================
        // FUNCTION: Open Chapter PDF Viewer
        // ============================================
        function openChapterPDF(num) {
            const chapterContent = document.getElementById('chapterContent');
            const pdfContainer = document.getElementById('chapterPDFContainer');
            const pdfViewer = document.getElementById('chapterPDFViewer');
            const pdfTitle = document.getElementById('chapterPDFTitle');
            chapterContent.style.display = 'none';
            pdfContainer.style.display = 'block';
            pdfTitle.textContent = `📖 अध्याय ${num} : ${chapters[num-1].title}`;
            pdfViewer.src = chapterPDFLinks[num];
            document.getElementById('chapterContentSection').scrollIntoView({ behavior: 'smooth' });
        }

        // ============================================
        // FUNCTION: Reset Chapter View (Hide PDF, Show Text)
        // ============================================
        function resetChapterView() {
            const chapterContent = document.getElementById('chapterContent');
            const pdfContainer = document.getElementById('chapterPDFContainer');
            chapterContent.style.display = 'block';
            pdfContainer.style.display = 'none';
            const pdfViewer = document.getElementById('chapterPDFViewer');
            if(pdfViewer) pdfViewer.src = '';
        }

        // ============================================
        // NOTE: Back button now uses anchor tag to courses.html
        // No separate goBack() function needed for navigation
        // The back button above is an <a href="courses.html"> tag
        // ============================================

        // ============================================
        // FUNCTION: Toggle Mobile Menu
        // ============================================
        function toggleMenu() { document.getElementById('menu').classList.toggle('show'); }

        // ============================================
        // QUIZ FUNCTIONS
        // ============================================
        function loadQuiz() {
            const quizArea = document.getElementById('quizArea');
            const q = questions[currentQuestion];
            let optionsHtml = '';
            q.options.forEach((opt, idx) => {
                optionsHtml += `<label class="option"><input type="radio" name="q" value="${idx}" ${userAnswers[currentQuestion] === idx ? 'checked' : ''}><span>${opt}</span></label>`;
            });
            quizArea.innerHTML = `<div class="question"><h3>${q.question}</h3><div class="options">${optionsHtml}</div></div>`;
            document.getElementById('quizProgress').textContent = `प्रश्न ${currentQuestion+1}/${questions.length}`;
            document.getElementById('prevBtn').disabled = currentQuestion === 0;
            document.getElementById('nextBtn').style.display = currentQuestion === questions.length-1 ? 'none' : 'inline-flex';
            document.getElementById('submitBtn').style.display = currentQuestion === questions.length-1 ? 'inline-flex' : 'none';
            document.querySelectorAll('input[name="q"]').forEach(radio => {
                radio.addEventListener('change', function() { userAnswers[currentQuestion] = parseInt(this.value); });
            });
        }

        function nextQuestion() { if(currentQuestion < questions.length-1) { currentQuestion++; loadQuiz(); } }
        function prevQuestion() { if(currentQuestion > 0) { currentQuestion--; loadQuiz(); } }
        
        function checkQuiz() {
            let score = 0;
            for(let i=0; i<questions.length; i++) if(userAnswers[i] === questions[i].correct) score++;
            const percentage = (score/questions.length)*100;
            const resultDiv = document.getElementById('result');
            resultDiv.style.display = 'block';
            if(percentage >= 60) {
                resultDiv.innerHTML = `<div style="background:#d4edda; color:#155724; padding:20px; border-radius:10px; text-align:center;"><i class="fas fa-trophy" style="font-size:48px; color:#ffd700;"></i><h3>बधाई हो!</h3><p>आपने ${score}/${questions.length} सही उत्तर दिए (${percentage}%)</p></div>`;
            } else {
                resultDiv.innerHTML = `<div style="background:#f8d7da; color:#721c24; padding:20px; border-radius:10px; text-align:center;"><i class="fas fa-book" style="font-size:48px;"></i><h3>पुनः प्रयास करें</h3><p>आपने ${score}/${questions.length} सही उत्तर दिए (${percentage}%)</p></div>`;
            }
        }

        // ============================================
        // FUNCTION: Switch Between Tabs
        // ============================================
        function switchTab(tabName, event) {
            document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
            document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
            event.target.classList.add('active');
            document.getElementById(tabName+'Tab').classList.add('active');
            resetChapterView();
        }

        // ============================================
        // BACK TO TOP FUNCTIONALITY
        // ============================================
        window.onscroll = function() {
            const backToTop = document.getElementById('backToTop');
            if(document.body.scrollTop > 200 || document.documentElement.scrollTop > 200) backToTop.classList.add('show');
            else backToTop.classList.remove('show');
        };
        function scrollToTop() { window.scrollTo({ top: 0, behavior: 'smooth' }); }

        // ============================================
        // INITIALIZE PAGE
        // ============================================
        loadChapters();
        loadQuiz();

        // ============================================
        // EXPOSE FUNCTIONS TO GLOBAL SCOPE
        // ============================================
        window.openChapterPDF = openChapterPDF;
        window.toggleMenu = toggleMenu;
        window.switchTab = switchTab;
        window.nextQuestion = nextQuestion;
        window.prevQuestion = prevQuestion;
        window.checkQuiz = checkQuiz;
        window.scrollToTop = scrollToTop;
        window.resetChapterView = resetChapterView;
    </script>
</body>

</html>